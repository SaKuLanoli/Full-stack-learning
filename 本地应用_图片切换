1.框架
用到：·图片数组
      ·索引
      ·v-on指令
      ·v-bind指令
      ·v-show指令

框架：1）定义图片数组
      2）添加图片索引
      3）绑定src属性
      4）图片切换逻辑
      5）显示状态切换

<div id="app">
    <img src="imgArr[index]">
    <a href="#" @click="prev" v-show="条件"></a>
    <a href="#" @click="next" v-show="条件"></a>
</div>

var app = new Vue({
      el:"#app",
      data:{
          imgArr:[],
          index:0
      },
      methods:{
          prev:function (){},
          next:function (){}
      }
  })
